node_modules/

# NOTE: dist/ is intentionally committed — it is the GitHub Action's runtime
# entrypoint (dist/ci.mjs), built from src/ci.js. Do not ignore it.

# Secrets — never commit. (.env.example is the committed template.)
.env
.env.local
.env.*.local
.env.production
.env.development


# Local clone workspace / reports
/workspace/
/reports/
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
